import React from "react";
import { View, Text, StyleSheet } from "react-native";

const SiginScreen = () => {
  return (
    <View>
      <Text style={{ fontSize: 48 }}>SiginScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default SiginScreen;
